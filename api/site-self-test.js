const SEMANTIC_AUDITS=new Set(['/api/trend-integrity-audit','/api/decision-integrity-audit','/api/autonomy-health']);
const SEMANTIC_FAILURE=new Set(['FAIL','CRITICAL','DEGRADED','STALE']);
const SEMANTIC_WAITING=new Set(['NO_DATA','WAITING_FOR_TRENDS','WAITING']);
async function probe(base,path){const started=Date.now();try{const r=await fetch(`${base}${path}`,{cache:'no-store',headers:{Accept:path.startsWith('/api/')?'application/json':'text/html','x-argus-health-check':'1'}});const contentType=r.headers.get('content-type')||'';let body=null;if(SEMANTIC_AUDITS.has(path)&&contentType.includes('application/json'))body=await r.json().catch(()=>null);const semanticFailure=SEMANTIC_AUDITS.has(path)&&body&&SEMANTIC_FAILURE.has(body.status),semanticWaiting=SEMANTIC_AUDITS.has(path)&&body&&SEMANTIC_WAITING.has(body.status);return{path,ok:r.ok&&!semanticFailure&&!semanticWaiting,status:r.status,ms:Date.now()-started,contentType,semanticStatus:body?.status||null,semanticState:semanticFailure?'FAIL':semanticWaiting?'WAITING':body?.status||null,checked:body?.checked??null,violations:semanticFailure?body.violations?.slice?.(0,10):undefined}}catch(e){return{path,ok:false,status:0,ms:Date.now()-started,error:e.message}}}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const proto=String(req.headers['x-forwarded-proto']||'https').split(',')[0],host=req.headers['x-forwarded-host']||req.headers.host;if(!host)return res.status(500).json({error:'Host unavailable'});const base=`${proto}://${host}`;
const paths=['/','/live.html','/daily-slip.html','/markets.html','/exact-scores.html','/trends.html','/shield.html','/prediction-results.html','/virtual-bankroll.html','/system-health.html','/historical-validation.html','/training-dashboard.html','/compte-rendu-des-predictions.html','/evolution-dashboard.html','/argus-lab.html','/developer-health.html','/manifest.webmanifest','/sw.js','/src/live-prime-follow.js','/src/live-command-center.js','/src/live-snapshot-guard.js','/src/surviving-edge-client.js','/src/pressure-window.js','/api/site-health','/api/autopilot-health','/api/autonomy-health','/api/learning-health','/api/developer-health','/api/deployment-verifier?latest=1','/api/prediction-ledger?limit=1','/api/pressure-window-memory','/api/trend-integrity-audit','/api/decision-integrity-audit'];
const checks=await Promise.all(paths.map(p=>probe(base,p))),failures=checks.filter(x=>!x.ok&&x.semanticState!=='WAITING'),waiting=checks.filter(x=>x.semanticState==='WAITING'),slow=checks.filter(x=>x.ok&&x.ms>2500).map(x=>({path:x.path,ms:x.ms}));return res.status(200).json({version:'SITE-SELF-TEST-28',generatedAt:new Date().toISOString(),status:failures.length?'DEGRADED':waiting.length?'WAITING_FOR_EVIDENCE':slow.length?'SLOW':'HEALTHY',checked:checks.length,failures,waiting,slow,checks,policy:{readOnly:true,publicObservability:true,secretValuesExposed:false,providerQuotaSpendAllowed:false,persistentWritesAllowed:false,mutatingJobsExcluded:true,autopilotHealthObservedWithoutTriggeringAutopilot:true,autonomyHeartbeatObserved:true,developerHealthObserved:true,deploymentSnapshotObserved:true,trendIntegrityObserved:true,decisionIntegrityObserved:true,virtualBankrollPageObserved:true,semanticNoDataIsNotHealthy:true}})}