import { readJson, writeJson, storageReady } from './_report-store.js';
const OUT='argus/health/deployment-verification.json';
const CRITICAL=['/api/site-health','/api/autopilot-health','/api/learning-health','/api/prediction-ledger?limit=1','/api/decision-integrity-audit','/api/policy-registry'];
function authorized(req){const s=String(process.env.CRON_SECRET||'').trim();return !s||req.headers.authorization===`Bearer ${s}`}
async function get(base,path,auth){const started=Date.now();try{const r=await fetch(`${base}${path}`,{headers:{Accept:'application/json',...(auth?{Authorization:auth}:{}),'x-argus-deployment-verifier':'1'},cache:'no-store'}),body=await r.json().catch(()=>null);return{path,ok:r.ok,status:r.status,ms:Date.now()-started,semanticStatus:body?.status||body?.summary?.status||null,error:body?.error||null}}catch(e){return{path,ok:false,status:0,ms:Date.now()-started,error:e.message}}}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(!storageReady())return res.status(503).json({error:'Deployment verifier storage unavailable'});if(req.method==='GET'&&String(req.query?.latest||'')==='1')return res.status(200).json(await readJson(OUT,{version:'DEPLOYMENT-VERIFIER-2',status:'UNKNOWN',generatedAt:null}));if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!authorized(req))return res.status(401).json({error:'Unauthorized'});const proto=String(req.headers['x-forwarded-proto']||'https').split(',')[0],host=req.headers['x-forwarded-host']||req.headers.host;if(!host)return res.status(500).json({error:'Host unavailable'});const productionHost=String(process.env.VERCEL_PROJECT_PRODUCTION_URL||'').trim().replace(/^https?:\/\//,'').replace(/\/$/,'');const base=productionHost?`https://${productionHost}`:`${proto}://${host}`,auth=String(req.headers.authorization||'');let selfTest=null;try{const r=await fetch(`${base}/api/site-self-test`,{headers:{Accept:'application/json'},cache:'no-store'});selfTest=await r.json().catch(()=>null)}catch(_){}const checks=await Promise.all(CRITICAL.map(p=>get(base,p,auth))),failed=checks.filter(x=>!x.ok),semanticFails=checks.filter(x=>String(x.semanticStatus||'').toUpperCase()==='FAIL'),selfFail=!selfTest||!['HEALTHY','SLOW'].includes(String(selfTest.status||''));let status='READY';if(failed.length||semanticFails.length||selfFail)status='BLOCKED';else if(checks.some(x=>x.ms>2500)||selfTest?.status==='SLOW')status='DEGRADED';const state={version:'DEPLOYMENT-VERIFIER-2',generatedAt:new Date().toISOString(),status,productionHost:new URL(base).host,vercel:{environment:process.env.VERCEL_ENV||null,gitCommitSha:process.env.VERCEL_GIT_COMMIT_SHA||null,deploymentId:process.env.VERCEL_DEPLOYMENT_ID||null},selfTest:{status:selfTest?.status||'UNAVAILABLE',checked:selfTest?.checked??null,failures:selfTest?.failures?.length??null,slow:selfTest?.slow?.length??null},critical:{checked:checks.length,failed:failed.length,semanticFails:semanticFails.length,checks},policy:{readOnlyChecks:true,persistentSnapshot:true,providerQuotaSpendAllowed:false,mutatingEndpointsExcluded:true,automaticRollback:false,automaticPromotion:false,readyRequiresCriticalHealth:true}};await writeJson(OUT,state);return res.status(200).json(state)}
