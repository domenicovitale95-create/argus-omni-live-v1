import { requestQuery } from './_request-query.js';
import { readJson, writeJson, storageReady } from './_report-store.js';
const TZ='Europe/Brussels';
function dateTZ(v=new Date()){const p=new Intl.DateTimeFormat('en-CA',{timeZone:TZ,year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(v);const m=Object.fromEntries(p.map(x=>[x.type,x.value]));return `${m.year}-${m.month}-${m.day}`}
function validOdds(m){return Number(m?.home)>1&&Number(m?.draw)>1&&Number(m?.away)>1}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(!storageReady())return res.status(503).json({error:'Market memory storage unavailable'});if(req.method==='GET'){const date=String(requestQuery(req)?.date||dateTZ());return res.status(200).json(await readJson(`argus/market-memory/${date}.json`,{date,fixtures:{}}))}if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const matches=Array.isArray(req.body?.matches)?req.body.matches:[];const now=new Date().toISOString();const groups=new Map();for(const m of matches){if(!m?.id||!validOdds(m.markets))continue;const date=dateTZ(new Date(m.kickoff||Date.now()));if(!groups.has(date))groups.set(date,[]);groups.get(date).push(m)}let saved=0;for(const[date,rows]of groups){const path=`argus/market-memory/${date}.json`;const store=await readJson(path,{date,timezone:TZ,fixtures:{}});store.fixtures||={};for(const m of rows){const id=String(m.id),f=store.fixtures[id]||{fixtureId:m.id,competition:m.competition,home:m.home,away:m.away,kickoff:m.kickoff,snapshots:[]};const snap={recordedAt:now,phase:m.isLive?'LIVE':'PREMATCH',minute:m.minute??null,status:m.status,odds:{home:Number(m.markets.home),draw:Number(m.markets.draw),away:Number(m.markets.away)}};const last=f.snapshots[f.snapshots.length-1];const sig=x=>`${x?.phase}|${x?.minute}|${x?.odds?.home}|${x?.odds?.draw}|${x?.odds?.away}`;if(!last||sig(last)!==sig(snap)){f.snapshots.push(snap);if(f.snapshots.length>240)f.snapshots=f.snapshots.slice(-240);saved++}store.fixtures[id]=f}store.updatedAt=now;await writeJson(path,store)}return res.status(200).json({ok:true,saved,dates:[...groups.keys()]})}