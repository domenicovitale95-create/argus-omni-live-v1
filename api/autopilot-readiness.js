import { listJson, readManyJson, readJson, storageReady } from './_report-store.js';

const cap=(v,min=0,max=100)=>Math.max(min,Math.min(max,v));
const pct=(v,d)=>d?Math.round(v/d*100):0;
function bucket(p){const pc=Math.max(0,Math.min(99.999,Number(p)*100)),lo=Math.floor(pc/5)*5;return `${lo}-${lo+5}`}
function brusselsClock(){const parts=new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/Brussels',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date()),m=Object.fromEntries(parts.map(x=>[x.type,x.value]));return{hour:Number(m.hour),minute:Number(m.minute)}}
function scheduledIdle(){const c=brusselsClock();return c.hour<6&&!(c.hour===0&&c.minute<=30)}
function automationEvidence(plan){const generated=plan?.generatedAt?new Date(plan.generatedAt).getTime():0,ageMinutes=generated?Math.max(0,Math.round((Date.now()-generated)/60000)):null,rows=Array.isArray(plan?.plan)?plan.plan.length:0,idle=scheduledIdle();let score=20,status='NO_PERSISTED_PLAN';if(generated){if(idle&&ageMinutes<=720){score=80;status='SCHEDULED_IDLE'}else if(ageMinutes<=35){score=95;status='RECENT_PLAN'}else if(ageMinutes<=180){score=80;status='PLAN_AVAILABLE'}else if(ageMinutes<=720){score=60;status='PLAN_STALE'}else{score=40;status='PLAN_VERY_STALE'}}return{score,status,ageMinutes,rows,generatedAt:plan?.generatedAt||null,scheduledIdle:idle}}
function decisionIntegrity(plan){let critical=0,high=0;for(const r of plan?.plan||[]){const v=String(r.finalVerdict||'NO BET'),active=Boolean(r.betEligible),decay=String(r.signalDecayStatus||''),move=String(r.marketMovement||''),timing=String(r.timingAction||''),edge=Number(r.eligibilityCandidate?.edgePct),conf=Number(r.netConfidence),stake=Number(r.recommendedStakePct||0);if(active&&!['PRIME','VALUE'].includes(v))critical++;if(active&&['EXPIRED','DECAYING'].includes(decay))critical++;if(active&&['REVERSAL','ADVERSE_STEAM'].includes(move))critical++;if(active&&r.preKickoffGate==='BLOCKED')critical++;if(active&&r.portfolioBlocked)critical++;if(!active&&stake>0)critical++;if(active&&timing==='WAIT')high++;if(v==='PRIME'&&Number.isFinite(edge)&&edge<6)high++;if(v==='PRIME'&&Number.isFinite(conf)&&conf<68)high++;if(v==='VALUE'&&Number.isFinite(edge)&&edge<3.5)high++;if(v==='VALUE'&&Number.isFinite(conf)&&conf<58)high++}const score=cap(100-critical*35-high*8);return{score,status:critical?'FAIL':high?'CAUTION':'PASS',critical,high}}
export default async function handler(req,res){res.setHeader('Cache-Control','s-maxage=120, stale-while-revalidate=300');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!storageReady())return res.status(200).json({score:15,status:'INFRASTRUCTURE BLOCKED',components:{storage:0},note:'Vercel Blob storage is not linked; learning cannot persist.'});const [reportBlobs,shadowBlobs,plan]=await Promise.all([listJson('argus/reports/',180),listJson('argus/shadow/',180),readJson('argus/autopilot/decision-plan.json',{plan:[],generatedAt:null})]),[reports,shadows]=await Promise.all([readManyJson(reportBlobs),readManyJson(shadowBlobs)]);let recordedSettled=0,recordedActionable=0,shadowSettled=0,shadowPriced=0,shadowPicks=0,clvN=0,clvSum=0,lateFreeze=0,missingFreeze=0,brierSum=0,calN=0;const marketKeys=new Set(),leagueKeys=new Set(),buckets={};for(const report of reports)for(const row of report.matches||[]){if(['WIN','LOSS'].includes(row.outcome)){recordedSettled++;if(row.prediction&&String(row.prediction.classification||'').toUpperCase()!=='NO BET')recordedActionable++;if(row.competition)leagueKeys.add(row.competition)}}for(const book of shadows)for(const f of Object.values(book.fixtures||{})){if(f.competition)leagueKeys.add(f.competition);const ko=new Date(f.kickoff||0).getTime(),fr=new Date(f.frozenAt||0).getTime();if(!f.frozenAt)missingFreeze++;else if(Number.isFinite(ko)&&Number.isFinite(fr)&&fr>=ko)lateFreeze++;for(const p of f.picks||[]){shadowPicks++;marketKeys.add(p.key);if(p.odds)shadowPriced++;if(['WIN','LOSS'].includes(p.outcome)){shadowSettled++;if(Number.isFinite(Number(p.clv))){clvN++;clvSum+=Number(p.clv)}const pr=Number(p.probability);if(Number.isFinite(pr)&&pr>0&&pr<1){const y=p.outcome==='WIN'?1:0;brierSum+=(pr-y)**2;calN++;const b=bucket(pr);if(!buckets[b])buckets[b]={n:0,p:0,y:0};buckets[b].n++;buckets[b].p+=pr;buckets[b].y+=y}}}}const clvCoverage=pct(clvN,shadowSettled),avgCLV=clvN?clvSum/clvN:null,freezeIntegrity=cap(100-lateFreeze*20-missingFreeze*10),brier=calN?brierSum/calN:null;let ece=0;if(calN)for(const b of Object.values(buckets)){ece+=(b.n/calN)*Math.abs(b.p/b.n-b.y/b.n)}const calibrationEvidence=cap(Math.round(calN/1.2)),calibrationAccuracy=calN?cap(Math.round(100-ece*500)):0,calibrationMaturity=Math.round(calibrationEvidence*.55+calibrationAccuracy*.45),auto=automationEvidence(plan),decisionAudit=decisionIntegrity(plan),integrityScore=Math.min(freezeIntegrity,decisionAudit.score);const components={dataPersistence:100,trackRecord:cap(recordedSettled),shadowLearning:cap(Math.round(shadowSettled/1.2)),marketCoverage:cap(Math.round((marketKeys.size/12)*100)),realPriceCoverage:cap(pct(shadowPriced,shadowPicks)),calibrationMaturity,leagueDiversity:cap(Math.round((leagueKeys.size/12)*100)),clvCoverage:cap(clvCoverage),integrity:integrityScore,automation:auto.score};const weights={dataPersistence:.08,trackRecord:.14,shadowLearning:.14,marketCoverage:.09,realPriceCoverage:.08,calibrationMaturity:.15,leagueDiversity:.05,clvCoverage:.10,integrity:.10,automation:.07};const score=Math.round(Object.entries(weights).reduce((s,[k,w])=>s+components[k]*w,0));const status=score>=85&&integrityScore>=95&&calibrationMaturity>=70?'AUTOPILOT READY FOR SUPERVISED USE':score>=70?'ADVANCED TRAINING':score>=50?'TRAINING IN PROGRESS':'EARLY TRAINING';const blockers=[];if(recordedSettled<50)blockers.push(`Need ${50-recordedSettled} more settled recorded predictions for a stronger real track record`);if(shadowSettled<100)blockers.push(`Need ${100-shadowSettled} more settled shadow predictions for broader validation`);if(components.realPriceCoverage<60)blockers.push('Real-price coverage across shadow markets is still limited');if(clvCoverage<60)blockers.push('Closing-line coverage is still too low for robust CLV validation');if(avgCLV!=null&&avgCLV<0)blockers.push('Average CLV is currently negative; ARGUS is not consistently beating the closing market');if(freezeIntegrity<95)blockers.push('Freeze-timing or persistence integrity gaps were detected');if(decisionAudit.critical)blockers.push(`Decision governance audit found ${decisionAudit.critical} critical contradiction(s); model promotion must remain frozen`);if(decisionAudit.high)blockers.push(`Decision governance audit found ${decisionAudit.high} high-severity inconsistency warning(s)`);if(calN<60)blockers.push('Probability calibration still needs more settled forecasts');if(ece>.08&&calN>=20)blockers.push('Calibration error remains high: predicted probabilities and observed frequencies diverge materially');if(marketKeys.size<8)blockers.push('More market families need settled evidence');if(auto.score<60&&!auto.scheduledIdle)blockers.push('Persisted Autopilot decision plan is missing or very stale; verify scheduler execution without disabling autonomy');return res.status(200).json({version:'AUTOPILOT-READINESS-6',generatedAt:new Date().toISOString(),score,status,components,automationEvidence:auto,decisionIntegrity:decisionAudit,evidence:{reports:reports.length,recordedSettled,recordedActionable,shadowBooks:shadows.length,shadowPicks,shadowSettled,shadowPriced,marketFamilies:marketKeys.size,leagues:leagueKeys.size,clvSamples:clvN,clvCoveragePct:clvCoverage,avgCLV:avgCLV==null?null:Number(avgCLV.toFixed(2)),lateFreeze,missingFreeze,calibrationSamples:calN,brier:brier==null?null:Number(brier.toFixed(4)),ecePct:calN?Number((ece*100).toFixed(1)):null},blockers,methodology:'Readiness combines persisted pre-kickoff evidence with decision-governance consistency. Critical contradictions cap integrity and must block model promotion. Scheduled idle time in Europe/Brussels does not count as an Autopilot fault. This does not authorize autonomous wagering.'})}
