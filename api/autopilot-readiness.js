import { listJson, readManyJson, storageReady } from './_report-store.js';

const cap=(v,min=0,max=100)=>Math.max(min,Math.min(max,v));
const pct=(v,d)=>d?Math.round(v/d*100):0;
function bucket(p){const pc=Math.max(0,Math.min(99.999,Number(p)*100)),lo=Math.floor(pc/5)*5;return `${lo}-${lo+5}`}
export default async function handler(req,res){
  res.setHeader('Cache-Control','s-maxage=120, stale-while-revalidate=300');
  if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});
  if(!storageReady())return res.status(200).json({score:15,status:'INFRASTRUCTURE BLOCKED',components:{storage:0},note:'Vercel Blob storage is not linked; learning cannot persist.'});
  const [reportBlobs,shadowBlobs]=await Promise.all([listJson('argus/reports/',180),listJson('argus/shadow/',180)]),[reports,shadows]=await Promise.all([readManyJson(reportBlobs),readManyJson(shadowBlobs)]);
  let recordedSettled=0,recordedActionable=0,shadowSettled=0,shadowPriced=0,shadowPicks=0,clvN=0,clvSum=0,lateFreeze=0,missingFreeze=0,brierSum=0,calN=0;const marketKeys=new Set(),leagueKeys=new Set(),buckets={};
  for(const report of reports)for(const row of report.matches||[]){if(['WIN','LOSS'].includes(row.outcome)){recordedSettled++;if(row.prediction&&String(row.prediction.classification||'').toUpperCase()!=='NO BET')recordedActionable++;if(row.competition)leagueKeys.add(row.competition)}}
  for(const book of shadows)for(const f of Object.values(book.fixtures||{})){if(f.competition)leagueKeys.add(f.competition);const ko=new Date(f.kickoff||0).getTime(),fr=new Date(f.frozenAt||0).getTime();if(!f.frozenAt)missingFreeze++;else if(Number.isFinite(ko)&&Number.isFinite(fr)&&fr>=ko)lateFreeze++;for(const p of f.picks||[]){shadowPicks++;marketKeys.add(p.key);if(p.odds)shadowPriced++;if(['WIN','LOSS'].includes(p.outcome)){shadowSettled++;if(Number.isFinite(Number(p.clv))){clvN++;clvSum+=Number(p.clv)}const pr=Number(p.probability);if(Number.isFinite(pr)&&pr>0&&pr<1){const y=p.outcome==='WIN'?1:0;brierSum+=(pr-y)**2;calN++;const b=bucket(pr);if(!buckets[b])buckets[b]={n:0,p:0,y:0};buckets[b].n++;buckets[b].p+=pr;buckets[b].y+=y}}}}
  const totalSettled=recordedSettled+shadowSettled,clvCoverage=pct(clvN,shadowSettled),avgCLV=clvN?clvSum/clvN:null,integrityScore=cap(100-lateFreeze*20-missingFreeze*10),brier=calN?brierSum/calN:null;
  let ece=0;if(calN)for(const b of Object.values(buckets)){ece+=(b.n/calN)*Math.abs(b.p/b.n-b.y/b.n)}
  const calibrationEvidence=cap(Math.round(calN/1.2)),calibrationAccuracy=calN?cap(Math.round(100-ece*500)):0,calibrationMaturity=Math.round(calibrationEvidence*.55+calibrationAccuracy*.45);
  const components={dataPersistence:100,trackRecord:cap(recordedSettled),shadowLearning:cap(Math.round(shadowSettled/1.2)),marketCoverage:cap(Math.round((marketKeys.size/12)*100)),realPriceCoverage:cap(pct(shadowPriced,shadowPicks)),calibrationMaturity,leagueDiversity:cap(Math.round((leagueKeys.size/12)*100)),clvCoverage:cap(clvCoverage),integrity:integrityScore,automation:95};
  const weights={dataPersistence:.08,trackRecord:.14,shadowLearning:.14,marketCoverage:.09,realPriceCoverage:.08,calibrationMaturity:.15,leagueDiversity:.05,clvCoverage:.10,integrity:.10,automation:.07};
  const score=Math.round(Object.entries(weights).reduce((s,[k,w])=>s+components[k]*w,0));
  const status=score>=85&&integrityScore>=95&&calibrationMaturity>=70?'AUTOPILOT READY FOR SUPERVISED USE':score>=70?'ADVANCED TRAINING':score>=50?'TRAINING IN PROGRESS':'EARLY TRAINING';
  const blockers=[];if(recordedSettled<50)blockers.push(`Need ${50-recordedSettled} more settled recorded predictions for a stronger real track record`);if(shadowSettled<100)blockers.push(`Need ${100-shadowSettled} more settled shadow predictions for broader validation`);if(components.realPriceCoverage<60)blockers.push('Real-price coverage across shadow markets is still limited');if(clvCoverage<60)blockers.push('Closing-line coverage is still too low for robust CLV validation');if(avgCLV!=null&&avgCLV<0)blockers.push('Average CLV is currently negative; ARGUS is not consistently beating the closing market');if(integrityScore<95)blockers.push('Integrity audit found freeze-timing or persistence gaps');if(calN<60)blockers.push('Probability calibration still needs more settled forecasts');if(ece>.08&&calN>=20)blockers.push('Calibration error remains high: predicted probabilities and observed frequencies diverge materially');if(marketKeys.size<8)blockers.push('More market families need settled evidence');
  return res.status(200).json({version:'AUTOPILOT-READINESS-3',generatedAt:new Date().toISOString(),score,status,components,evidence:{reports:reports.length,recordedSettled,recordedActionable,shadowBooks:shadows.length,shadowPicks,shadowSettled,shadowPriced,marketFamilies:marketKeys.size,leagues:leagueKeys.size,clvSamples:clvN,clvCoveragePct:clvCoverage,avgCLV:avgCLV==null?null:Number(avgCLV.toFixed(2)),lateFreeze,missingFreeze,calibrationSamples:calN,brier:brier==null?null:Number(brier.toFixed(4)),ecePct:calN?Number((ece*100).toFixed(1)):null},blockers,methodology:'Readiness is a governance heuristic derived from persisted, pre-kickoff frozen evidence. Calibration quality, CLV and integrity materially affect readiness. It is not a profitability guarantee and does not authorize automatic wagering.'});
}