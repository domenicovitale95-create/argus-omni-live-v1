import { listJson, readManyJson, storageReady } from './_report-store.js';

const cap=(v,min=0,max=100)=>Math.max(min,Math.min(max,v));
const pct=(v,d)=>d?Math.round(v/d*100):0;
export default async function handler(req,res){
  res.setHeader('Cache-Control','s-maxage=120, stale-while-revalidate=300');
  if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});
  if(!storageReady())return res.status(200).json({score:15,status:'INFRASTRUCTURE BLOCKED',components:{storage:0},note:'Vercel Blob storage is not linked; learning cannot persist.'});
  const [reportBlobs,shadowBlobs]=await Promise.all([listJson('argus/reports/',180),listJson('argus/shadow/',180)]),[reports,shadows]=await Promise.all([readManyJson(reportBlobs),readManyJson(shadowBlobs)]);
  let recordedSettled=0,recordedActionable=0,shadowSettled=0,shadowPriced=0,shadowPicks=0,clvN=0,clvSum=0,lateFreeze=0,missingFreeze=0;const marketKeys=new Set(),leagueKeys=new Set();
  for(const report of reports)for(const row of report.matches||[]){if(['WIN','LOSS'].includes(row.outcome)){recordedSettled++;if(row.prediction&&String(row.prediction.classification||'').toUpperCase()!=='NO BET')recordedActionable++;if(row.competition)leagueKeys.add(row.competition)}}
  for(const book of shadows)for(const f of Object.values(book.fixtures||{})){if(f.competition)leagueKeys.add(f.competition);const ko=new Date(f.kickoff||0).getTime(),fr=new Date(f.frozenAt||0).getTime();if(!f.frozenAt)missingFreeze++;else if(Number.isFinite(ko)&&Number.isFinite(fr)&&fr>=ko)lateFreeze++;for(const p of f.picks||[]){shadowPicks++;marketKeys.add(p.key);if(p.odds)shadowPriced++;if(['WIN','LOSS'].includes(p.outcome)){shadowSettled++;if(Number.isFinite(Number(p.clv))){clvN++;clvSum+=Number(p.clv)}}}}
  const totalSettled=recordedSettled+shadowSettled,clvCoverage=pct(clvN,shadowSettled),avgCLV=clvN?clvSum/clvN:null;
  const integrityScore=cap(100-lateFreeze*20-missingFreeze*10);
  const components={dataPersistence:100,trackRecord:cap(recordedSettled),shadowLearning:cap(Math.round(shadowSettled/1.2)),marketCoverage:cap(Math.round((marketKeys.size/12)*100)),realPriceCoverage:cap(pct(shadowPriced,shadowPicks)),calibrationMaturity:cap(totalSettled),leagueDiversity:cap(Math.round((leagueKeys.size/12)*100)),clvCoverage:cap(clvCoverage),integrity:integrityScore,automation:95};
  const weights={dataPersistence:.08,trackRecord:.15,shadowLearning:.15,marketCoverage:.10,realPriceCoverage:.08,calibrationMaturity:.12,leagueDiversity:.05,clvCoverage:.10,integrity:.10,automation:.07};
  const score=Math.round(Object.entries(weights).reduce((s,[k,w])=>s+components[k]*w,0));
  const status=score>=85&&integrityScore>=95?'AUTOPILOT READY FOR SUPERVISED USE':score>=70?'ADVANCED TRAINING':score>=50?'TRAINING IN PROGRESS':'EARLY TRAINING';
  const blockers=[];if(recordedSettled<50)blockers.push(`Need ${50-recordedSettled} more settled recorded predictions for a stronger real track record`);if(shadowSettled<100)blockers.push(`Need ${100-shadowSettled} more settled shadow predictions for broader validation`);if(components.realPriceCoverage<60)blockers.push('Real-price coverage across shadow markets is still limited');if(clvCoverage<60)blockers.push('Closing-line coverage is still too low for robust CLV validation');if(avgCLV!=null&&avgCLV<0)blockers.push('Average CLV is currently negative; ARGUS is not consistently beating the closing market');if(integrityScore<95)blockers.push('Integrity audit found freeze-timing or persistence gaps');if(marketKeys.size<8)blockers.push('More market families need settled evidence');
  return res.status(200).json({version:'AUTOPILOT-READINESS-2',generatedAt:new Date().toISOString(),score,status,components,evidence:{reports:reports.length,recordedSettled,recordedActionable,shadowBooks:shadows.length,shadowPicks,shadowSettled,shadowPriced,marketFamilies:marketKeys.size,leagues:leagueKeys.size,clvSamples:clvN,clvCoveragePct:clvCoverage,avgCLV:avgCLV==null?null:Number(avgCLV.toFixed(2)),lateFreeze,missingFreeze},blockers,methodology:'Readiness is a governance heuristic derived from persisted, pre-kickoff frozen evidence. CLV and integrity now materially affect readiness. It is not a profitability guarantee and does not authorize automatic wagering.'});
}
