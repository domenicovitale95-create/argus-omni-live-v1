import { listJson, readManyJson, readJson, storageReady } from './_report-store.js';
const PATH='argus/learning/ledger-diagnostics.json';
function n(v){const x=Number(v);return Number.isFinite(x)?x:null}
function dayDiff(a,b){const x=new Date(a).getTime(),y=new Date(b).getTime();return Number.isFinite(x)&&Number.isFinite(y)?Math.abs(x-y)/86400000:null}
function level(s){return s>=85?'HEALTHY':s>=65?'LEARNING':s>=40?'WEAK':'INSUFFICIENT'}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!storageReady())return res.status(503).json({error:'Storage unavailable'});const [diagnostics,reportBlobs,learningBlobs]=await Promise.all([readJson(PATH,null),listJson('argus/reports/',400),listJson('argus/learning/',80)]),[reports,learning]=await Promise.all([readManyJson(reportBlobs),readManyJson(learningBlobs)]),forensics=learning.find(x=>x?.version==='POSTMATCH-FORENSICS-1')||{},dated=reports.filter(x=>x?.date).sort((a,b)=>new Date(a.date)-new Date(b.date)),first=dated[0]?.date||null,last=dated[dated.length-1]?.date||null;let settled=0,predicted=0,withModel=0,withOdds=0,withConfidence=0,withEdge=0;const competitions=new Set();for(const r of reports)for(const m of r.matches||[]){if(!m.prediction)continue;predicted++;if(m.competition)competitions.add(m.competition);if(m.prediction.model)withModel++;if(n(m.prediction.odds)!=null)withOdds++;if(n(m.prediction.confidence)!=null)withConfidence++;if(n(m.prediction.edge)!=null)withEdge++;if(['WIN','LOSS'].includes(m.outcome))settled++}const coverage={model:predicted?withModel/predicted:0,odds:predicted?withOdds/predicted:0,confidence:predicted?withConfidence/predicted:0,edge:predicted?withEdge/predicted:0},span=first&&last?dayDiff(first,last):0,fresh=last?dayDiff(last,new Date().toISOString()):999;let score=Math.min(30,settled/100*30)+Math.min(15,competitions.size/10*15)+15*coverage.model+10*coverage.odds+10*coverage.confidence+10*coverage.edge+Math.min(5,(span||0)/60*5)+(fresh<=2?5:fresh<=7?3:0);score=Math.round(score);const blockers=[];if(settled<20)blockers.push('SETTLED_SAMPLE_LT_20');if(coverage.model<.8)blockers.push('MODEL_COVERAGE_LT_80');if(coverage.odds<.7)blockers.push('ODDS_COVERAGE_LT_70');if(coverage.confidence<.7)blockers.push('CONFIDENCE_COVERAGE_LT_70');if(fresh>7)blockers.push('LEARNING_DATA_STALE');const diagAge=diagnostics?Date.now()-new Date(diagnostics.generatedAt||0).getTime():NaN;return res.status(200).json({version:'LEARNING-HEALTH-2',generatedAt:new Date().toISOString(),score,status:level(score),available:predicted>0,sample:{reports:reports.length,predicted,settled,competitions:competitions.size,firstDate:first,lastDate:last,historyDays:span==null?null:Number(span.toFixed(1)),freshnessDays:Number(fresh.toFixed(1))},coverage:Object.fromEntries(Object.entries(coverage).map(([k,v])=>[k,Number((v*100).toFixed(1))])),forensics:{settled:forensics.settled||0,generatedAt:forensics.generatedAt||null},ledgerDiagnostics:{available:Boolean(diagnostics),status:diagnostics?.global?.status||null,totalSettled:diagnostics?.totalSettled||0,stale:!Number.isFinite(diagAge)||diagAge>86400000},blockers,policy:{readOnly:true,writes:false,recompute:false,adaptivePromotionAllowed:score>=85&&settled>=100&&blockers.length===0,rule:'Learning health measures evidence readiness only. It cannot promote bets, loosen governance, or modify probabilities.'}})}
