import { readJson, writeJson, listJson, storageReady } from './_report-store.js';

const API='https://v3.football.api-sports.io';
const ARCHIVE='argus/research/historical-decade-fixtures.json';
const OUT='argus/research/historical-stats-enrichment.json';
const DEFAULT_BATCH=4,MAX_BATCH=8;
const MAX_MONOLITH_BYTES=24*1024*1024;
function authorized(req){const s=process.env.CRON_SECRET;return !s||req.headers.authorization===`Bearer ${s}`}
function qh(h){const d=Number(h.get('x-ratelimit-requests-remaining')),l=Number(h.get('x-ratelimit-requests-limit')),m=Number(h.get('x-ratelimit-remaining'));return{dailyRemaining:Number.isFinite(d)?d:null,dailyLimit:Number.isFinite(l)?l:null,minuteRemaining:Number.isFinite(m)?m:null}}
function reserve(q){return Number.isFinite(q?.dailyLimit)?Math.max(250,Math.ceil(q.dailyLimit*.15)):500}
function val(stats,name){const x=(stats||[]).find(s=>String(s.type||'').toLowerCase()===String(name).toLowerCase())?.value;if(x==null)return null;const n=Number(String(x).replace('%',''));return Number.isFinite(n)?n:null}
async function getStats(id){const key=process.env.API_FOOTBALL_KEY;if(!key)throw new Error('API_FOOTBALL_KEY is not configured');const r=await fetch(`${API}/fixtures/statistics?fixture=${id}`,{headers:{'x-apisports-key':key,Accept:'application/json'}});const quota=qh(r.headers);if(!r.ok)throw new Error(`HTTP ${r.status}`);const j=await r.json();if(j?.errors&&Object.keys(j.errors).length)throw new Error(JSON.stringify(j.errors));const b=j.response||[],home=b[0]?.statistics||[],away=b[1]?.statistics||[];return{quota,data:{home:{corners:val(home,'Corner Kicks'),yellow:val(home,'Yellow Cards'),red:val(home,'Red Cards'),shots:val(home,'Total Shots'),shotsOnTarget:val(home,'Shots on Goal'),possession:val(home,'Ball Possession'),fouls:val(home,'Fouls')},away:{corners:val(away,'Corner Kicks'),yellow:val(away,'Yellow Cards'),red:val(away,'Red Cards'),shots:val(away,'Total Shots'),shotsOnTarget:val(away,'Shots on Goal'),possession:val(away,'Ball Possession'),fouls:val(away,'Fouls')}}}}
function derive(s){const h=s.home||{},a=s.away||{},sum=(x,y)=>Number.isFinite(x)&&Number.isFinite(y)?x+y:null;return{cornersTotal:sum(h.corners,a.corners),homeCorners:h.corners,awayCorners:a.corners,yellowCardsTotal:sum(h.yellow,a.yellow),redCardsTotal:sum(h.red,a.red),shotsTotal:sum(h.shots,a.shots),shotsOnTargetTotal:sum(h.shotsOnTarget,a.shotsOnTarget),foulsTotal:sum(h.fouls,a.fouls),possessionHome:h.possession,possessionAway:a.possession}}
export default async function handler(req,res){
  res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!authorized(req))return res.status(401).json({error:'Unauthorized'});if(!storageReady())return res.status(503).json({error:'Storage unavailable'});
  const archiveMeta=(await listJson(ARCHIVE,10)).find(b=>b.pathname===ARCHIVE)||null;
  const archiveSizeBytes=Number.isFinite(Number(archiveMeta?.size))?Number(archiveMeta.size):null;
  if(archiveSizeBytes!=null&&archiveSizeBytes>MAX_MONOLITH_BYTES)return res.status(200).json({ok:true,version:'HISTORICAL-STATS-ENRICHMENT-3',status:'PAUSED_MEMORY_GUARD',archiveSizeBytes,maxMonolithBytes:MAX_MONOLITH_BYTES,processed:0,providerCalls:0,migrationRequired:true,policy:{failClosed:true,noOOMRiskAccepted:true,noProviderQuotaSpend:true}});
  const a=await readJson(ARCHIVE,null);if(!a)return res.status(200).json({version:'HISTORICAL-STATS-ENRICHMENT-3',status:'WAITING_FOR_DECADE_ARCHIVE'});
  const store=await readJson(OUT,{version:'HISTORICAL-STATS-ENRICHMENT-3',fixtures:{}});store.fixtures||={};const rows=Object.values(a.fixtures||{}).sort((x,y)=>(y.timestamp||0)-(x.timestamp||0)),pending=rows.filter(f=>!store.fixtures[String(f.fixtureId)]);let processed=0,saved=0,lastQuota=null,errors=[];const batch=Math.max(1,Math.min(MAX_BATCH,Number(req.query?.fixtures)||DEFAULT_BATCH));
  for(const f of pending){if(processed>=batch)break;const k=String(f.fixtureId);try{const out=await getStats(f.fixtureId);lastQuota=out.quota;const d=derive(out.data),available=Object.values(d).some(Number.isFinite);store.fixtures[k]={fixtureId:f.fixtureId,date:f.date,competition:f.competition,home:f.home,away:f.away,status:available?'AVAILABLE':'NOT_AVAILABLE',stats:out.data,derived:d,savedAt:new Date().toISOString()};processed++;saved++;if(lastQuota?.dailyRemaining!=null&&lastQuota.dailyRemaining<=reserve(lastQuota))break;if(lastQuota?.minuteRemaining!=null&&lastQuota.minuteRemaining<=3)break}catch(e){errors.push({fixtureId:f.fixtureId,error:e.message});break}}
  store.version='HISTORICAL-STATS-ENRICHMENT-3';store.updatedAt=new Date().toISOString();store.available=Object.values(store.fixtures).filter(x=>x.status==='AVAILABLE').length;store.unavailable=Object.values(store.fixtures).filter(x=>x.status==='NOT_AVAILABLE').length;store.pending=Math.max(0,rows.length-Object.keys(store.fixtures).length);await writeJson(OUT,store);return res.status(errors.length?207:200).json({ok:errors.length===0,version:'HISTORICAL-STATS-ENRICHMENT-3',strategy:'NEWEST_UNENRICHED_FIRST',processed,saved,totalHistoricalFixtures:rows.length,enriched:Object.keys(store.fixtures).length,pending:store.pending,available:store.available,unavailable:store.unavailable,quota:lastQuota,reserve:reserve(lastQuota),archiveSizeBytes,errors,policy:{quotaAware:true,persistent:true,newestUnenrichedFirst:true,noCursorSkipping:true,noFabrication:true,memoryGuard:true,marketsEnabledWhenAvailable:['CORNERS','CARDS','SHOTS','SHOTS_ON_TARGET','FOULS','POSSESSION']}})
}
