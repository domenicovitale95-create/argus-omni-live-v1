import { readJson, storageReady } from './_report-store.js';
const PATHS={deployment:'argus/health/deployment-verification.json',selfImprovement:'argus/self-improvement/latest.json',governance:'argus/governance/latest.json',scheduler:'argus/autopilot/decision-plan.json',autopilot:'argus/health/autopilot.json',ledgerCron:'argus/health/prediction-ledger-cron.json'};
function ageMinutes(ts){const t=new Date(ts||0).getTime();return Number.isFinite(t)&&t>0?Math.max(0,Math.round((Date.now()-t)/60000)):null}
function freshness(age,limit){if(age==null)return'UNKNOWN';return age<=limit?'FRESH':age<=limit*2?'AGING':'STALE'}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!storageReady())return res.status(503).json({error:'Developer health storage unavailable'});const [deployment,selfImprovement,governance,scheduler,autopilot,ledgerCron]=await Promise.all(Object.values(PATHS).map(p=>readJson(p,null).catch(()=>null)));const depAge=ageMinutes(deployment?.generatedAt),siAge=ageMinutes(selfImprovement?.generatedAt),govAge=ageMinutes(governance?.generatedAt),schedAge=ageMinutes(scheduler?.generatedAt),autoAge=ageMinutes(autopilot?.completedAt||autopilot?.generatedAt),ledgerAge=ageMinutes(ledgerCron?.completedAt||ledgerCron?.generatedAt);const components={deployment:{status:deployment?.status||'UNKNOWN',ageMinutes:depAge,freshness:freshness(depAge,20),commit:deployment?.vercel?.gitCommitSha||null,failures:deployment?.critical?.failed??null},autopilot:{status:autopilot?.ok===false?'DEGRADED':autopilot?'HEALTHY':'UNKNOWN',ageMinutes:autoAge,freshness:freshness(autoAge,15)},scheduler:{status:scheduler?.generatedAt?'AVAILABLE':'UNKNOWN',ageMinutes:schedAge,freshness:freshness(schedAge,30),prime:scheduler?.summary?.prime??null,value:scheduler?.summary?.value??null,eligible:scheduler?.summary?.eligible??null},ledger:{status:ledgerCron?.ok===false?'DEGRADED':ledgerCron?'HEALTHY':'UNKNOWN',ageMinutes:ledgerAge,freshness:freshness(ledgerAge,15),capture:ledgerCron?.capture?.status??null},selfImprovement:{status:selfImprovement?.ok===false?'DEGRADED':selfImprovement?'HEALTHY':'UNKNOWN',ageMinutes:siAge,freshness:freshness(siAge,390),promotionFreeze:Boolean(selfImprovement?.promotionFreeze),errors:selfImprovement?.errors?.length||0},governance:{status:governance?.ok===false?'DEGRADED':governance?'HEALTHY':'UNKNOWN',ageMinutes:govAge,freshness:freshness(govAge,390),promotionCandidates:governance?.summary?.promotionCandidates??null,rollbackRequired:Boolean(governance?.summary?.rollbackRequired)}};const vals=Object.values(components),criticalBad=[components.deployment,components.autopilot,components.scheduler,components.ledger].some(x=>['DEGRADED','BLOCKED'].includes(x.status)||x.freshness==='STALE'),unknown=vals.filter(x=>x.status==='UNKNOWN').length,stale=vals.filter(x=>x.freshness==='STALE').length;const status=criticalBad?'ACTION_REQUIRED':unknown>=3?'INCOMPLETE':stale?'ATTENTION':'HEALTHY';const priorities=[];if(components.deployment.status!=='READY')priorities.push('VERIFY_DEPLOYMENT');if(components.autopilot.freshness==='STALE')priorities.push('AUTOPILOT_STALE');if(components.ledger.freshness==='STALE')priorities.push('LEDGER_CRON_STALE');if(components.selfImprovement.errors)priorities.push('SELF_IMPROVEMENT_ERRORS');if(components.governance.rollbackRequired)priorities.push('POLICY_ROLLBACK_REVIEW');if(!priorities.length)priorities.push('NO_BLOCKER_DETECTED');return res.status(200).json({ok:true,version:'DEVELOPER-HEALTH-1',generatedAt:new Date().toISOString(),status,summary:{unknown,stale,priority:priorities[0]},priorities,components,policy:{readOnly:true,noProviderQuotaSpend:true,snapshotsOnly:true,designedForFastDeveloperTriage:true}})}
