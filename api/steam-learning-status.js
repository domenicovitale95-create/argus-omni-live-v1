import { readJson, storageReady } from './_report-store.js';
export default async function handler(req,res){res.setHeader('Cache-Control','s-maxage=180, stale-while-revalidate=600');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!storageReady())return res.status(503).json({error:'Storage unavailable'});const data=await readJson('argus/learning/steam-learning.json',null);if(!data)return res.status(200).json({version:'STEAM-LEARNING-1',generatedAt:null,settledRows:0,profiles:{byType:{},byLeagueType:{},byTiming:{},byLeagueTiming:{}},status:'WAITING_FOR_FIRST_LEARNING_RUN'});return res.status(200).json({...data,status:'READY'})}
