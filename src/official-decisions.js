(()=>{
  const finite=v=>Number.isFinite(Number(v))?Number(v):null;
  const humanSelection=v=>{const s=String(v||'').toUpperCase(),map={HOME:'Home team to win',DRAW:'Draw',AWAY:'Away team to win',OVER_1_5:'Over 1.5 goals',UNDER_1_5:'Under 1.5 goals',OVER_2_5:'Over 2.5 goals',UNDER_2_5:'Under 2.5 goals',OVER_3_5:'Over 3.5 goals',UNDER_3_5:'Under 3.5 goals',BTTS_YES:'Both teams to score · Yes',BTTS_NO:'Both teams to score · No',HOME_OVER_0_5:'Home team over 0.5 goals',HOME_UNDER_0_5:'Home team under 0.5 goals',AWAY_OVER_0_5:'Away team over 0.5 goals',AWAY_UNDER_0_5:'Away team under 0.5 goals',DOUBLE_CHANCE_1X:'Double chance 1X',DOUBLE_CHANCE_12:'Double chance 12',DOUBLE_CHANCE_X2:'Double chance X2'};if(map[s])return map[s];const exact=s.match(/^EXACT_SCORE:(\d+)-(\d+)$/);return exact?`Exact score ${exact[1]}-${exact[2]}`:(v||'No bet')};
  function telemetry(type,detail={}){try{document.dispatchEvent(new CustomEvent('argus:telemetry-error',{detail:{type,at:new Date().toISOString(),...detail}}))}catch(_){}try{console.warn(`[ARGUS ${type}]`,detail)}catch(_){}}
  function shadowEstimate(match){try{const raw=window.ArgusEngine?.analyze?.(match);if(!raw)return null;return{classification:raw.classification||'NO BET',bestMarket:raw.bestMarket||null,edge:finite(raw.edge),confidence:finite(raw.confidence),fairOdds:finite(raw.fairOdds),marketOdds:finite(raw.marketOdds),modelVersion:raw.modelVersion||null,validationStatus:raw.validationStatus||null,shadowOnly:true}}catch(_){return null}}
  function unavailable(match,base,reason='OFFICIAL_DECISION_UNAVAILABLE'){return{...base,classification:'NO BET',bestMarket:'No bet',selectionKey:null,marketType:null,marketOdds:null,fairOdds:null,edge:null,confidence:null,quality:finite(base?.quality),marketAvailable:false,betEligible:false,decisionSource:'OFFICIAL_DECISION_REQUIRED',engineStatus:`NO BET · ${reason}. Local estimates cannot create an actionable recommendation.`,shadowEstimate:shadowEstimate(match),integrityBlocker:reason};}
  function mathCheck(c={}){const p=finite(c.probability),o=finite(c.odds),mp=finite(c.marketProbability),fair=finite(c.fairOdds),edge=finite(c.edgePct),ev=finite(c.evPct),errors=[];if(!(p>0&&p<1))errors.push('PROBABILITY_OUT_OF_RANGE');if(!(o>1))errors.push('ODDS_INVALID');if(p>0&&fair!=null&&Math.abs(fair-1/p)>Math.max(.03,(1/p)*.015))errors.push('FAIR_ODDS_MISMATCH');if(p!=null&&mp!=null&&edge!=null&&Math.abs(edge-(p-mp)*100)>.2)errors.push('EDGE_MISMATCH');if(p!=null&&o>1&&ev!=null&&Math.abs(ev-(p*o-1)*100)>.2)errors.push('EV_MISMATCH');return{ok:errors.length===0,errors,p,o,mp,fair,edge,ev};}
  function liveStateCheck(match,row,c,age){if(!match?.isLive)return{ok:true};if(match?.dataStale)return{ok:false,reason:'LIVE_CLIENT_SNAPSHOT_STALE'};if(age==null||age>45)return{ok:false,reason:'LIVE_SCHEDULER_SNAPSHOT_NOT_FRESH_ENOUGH'};const currentMinute=finite(match?.minute),decisionMinute=finite(row?.minute);if(currentMinute!=null&&decisionMinute!=null&&Math.abs(currentMinute-decisionMinute)>1)return{ok:false,reason:'LIVE_MINUTE_MISMATCH'};const cs=c?.currentScore,ms=match?.score;if(cs&&ms&&(finite(cs.home)!==finite(ms.home)||finite(cs.away)!==finite(ms.away)))return{ok:false,reason:'LIVE_SCORE_MISMATCH'};const observedAge=finite(c?.observedAgeSeconds),maxAge=currentMinute!=null&&currentMinute>=80?45:75;if(observedAge==null||observedAge>maxAge)return{ok:false,reason:'LIVE_PROVIDER_SNAPSHOT_TOO_OLD'};return{ok:true};}
  function officialFromRow(match,base,row){
    if(!row)return unavailable(match,base);
    const generatedAt=row._schedulerGeneratedAt||null,age=generatedAt?Math.max(0,(Date.now()-new Date(generatedAt).getTime())/1000):null,c=row.eligibilityCandidate||{},stateCheck=liveStateCheck(match,row,c,age);
    if(!stateCheck.ok)return unavailable(match,base,stateCheck.reason);
    const verdict=String(row.finalVerdict||'NO BET').toUpperCase(),selection=row.stakeSelection||c.selection||c.side||null,odds=finite(row.stakeOdds??c.odds),prob=finite(c.probability),edge=finite(c.edgePct),fair=finite(c.fairOdds),ev=finite(c.evPct),confidence=finite(row.netConfidence),quality=finite(c.dataQuality),label=c.label||humanSelection(selection),math=mathCheck({...c,odds}),brain=row.centralBrain||null,eligible=Boolean(row.betEligible)&&['PRIME','VALUE'].includes(verdict);
    if(['PRIME','VALUE'].includes(verdict)&&!math.ok)return unavailable(match,base,`OFFICIAL_DECISION_MATH_INTEGRITY_FAILURE:${math.errors.join('|')}`);
    if(['PRIME','VALUE'].includes(verdict)&&(ev==null||ev<=0||fair==null||odds==null||odds<=fair))return unavailable(match,base,'OFFICIAL_DECISION_NON_POSITIVE_EXPECTED_VALUE');
    if(match?.isLive&&c.source!=='LIVE_V2')return unavailable(match,base,'LIVE_CANONICAL_MODEL_REQUIRED');
    if(match?.isLive&&c.validationStatus!=='VALIDATED'&&['PRIME','VALUE'].includes(verdict))return unavailable(match,base,'LIVE_V2_SHADOW_ONLY');
    return{...base,classification:verdict,bestMarket:verdict==='NO BET'?'No bet':label,selectionKey:selection,marketType:c.marketType||null,marketLine:c.line??null,marketOdds:odds,fairOdds:fair,edge,confidence:confidence??base?.confidence,quality:quality??base?.quality,rawProbability:prob??base?.rawProbability,shrunkProbability:prob??base?.shrunkProbability,conservativeProbability:prob??base?.conservativeProbability,conservativeEV:ev,marketAvailable:Boolean(odds&&odds>1),betEligible:eligible,recommendedStakePct:finite(row.recommendedStakePct),recommendedUnits:finite(row.recommendedUnits),centralBrain:brain,brainAction:brain?.action||null,brainMode:brain?.systemMode||null,brainReasons:Array.isArray(brain?.systemReasons)?brain.systemReasons:[],brainLocalHardRisk:Array.isArray(brain?.localHardRisk)?brain.localHardRisk:[],brainLocalSoftRisk:Array.isArray(brain?.localSoftRisk)?brain.localSoftRisk:[],decisionSource:'OFFICIAL_DECISION_SCHEDULER',engineStatus:verdict==='NO BET'?'ARGUS checked the official evidence and found no eligible bet.':'ARGUS selected the strongest official option after integrity checks.',outcomeProbability:prob,decisionConfidence:confidence,schedulerAgeSeconds:age,modelVersion:c.modelVersion||null,validationStatus:c.validationStatus||null,evidenceFreshness:row.evidenceFreshness||null,dataCompleteness:c.dataCompleteness??null,integrity:{math:math.ok,stateBound:true,centralBrainGate:Boolean(brain),source:'OFFICIAL_DECISION_SCHEDULER'}};
  }
  async function planMap(){try{const r=await fetch('/api/decision-scheduler',{cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok){telemetry('SCHEDULER_HTTP_FAILURE',{status:r.status});return new Map()}const j=await r.json(),rows=Array.isArray(j?.plan)?j.plan:[],generatedAt=j?.generatedAt||null;return new Map(rows.map(x=>[String(x.fixtureId),{...x,_schedulerGeneratedAt:generatedAt}]))}catch(error){telemetry('SCHEDULER_FETCH_FAILURE',{message:error?.message});return new Map()}}
  const ARCHIVE_BATCH_SIZE=20;
  async function archive(matches,analyses,meta){
    if(!Array.isArray(matches)||!Array.isArray(analyses)||!matches.length||matches.length!==analyses.length){telemetry('PREDICTION_ARCHIVE_CLIENT_PAYLOAD_INVALID',{matches:Array.isArray(matches)?matches.length:null,analyses:Array.isArray(analyses)?analyses.length:null});return}
    for(let start=0;start<matches.length;start+=ARCHIVE_BATCH_SIZE){
      const batchMatches=matches.slice(start,start+ARCHIVE_BATCH_SIZE),batchAnalyses=analyses.slice(start,start+ARCHIVE_BATCH_SIZE);
      try{
        const r=await fetch('/api/predictions',{method:'POST',headers:{'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify({matches:batchMatches,analyses:batchAnalyses,meta})});
        if(!r.ok){const detail=await r.json().catch(()=>null);telemetry('PREDICTION_ARCHIVE_HTTP_FAILURE',{status:r.status,batchStart:start,batchSize:batchMatches.length,serverError:detail?.error||null});return}
      }catch(error){telemetry('PREDICTION_ARCHIVE_FAILURE',{message:error?.message,batchStart:start,batchSize:batchMatches.length});return}
    }
  }
  function enforceMetricSemantics(){const conf=document.getElementById('osV2Confidence');if(conf&&/%\s*$/.test(conf.textContent||''))conf.textContent=(conf.textContent||'').replace(/%\s*$/,'/100');const edge=document.getElementById('osV2Edge');if(edge)edge.textContent=(edge.textContent||'').replace(/value advantage/gi,'model edge').replace(/ pts\b/gi,' pp');const fair=document.getElementById('osV2Fair');if(fair){const text=fair.textContent||'',m=text.match(/fair odds\s+([0-9]+(?:\.[0-9]+)?)/i),o=m?Number(m[1]):null;if(o&&o>1&&!/outcome p/i.test(text))fair.textContent=`ARGUS fair odds ${o.toFixed(2)} · outcome p ${(100/o).toFixed(1)}%`}document.querySelectorAll('.os-v2-metric > span').forEach(el=>{const t=(el.textContent||'').trim().toLowerCase();if(t==='confidence')el.textContent='Decision confidence';if(t==='data reliability')el.textContent='Evidence quality'});}
  let semanticQueued=false;function queueSemantics(){if(semanticQueued)return;semanticQueued=true;requestAnimationFrame(()=>{semanticQueued=false;enforceMetricSemantics()})}
  const originalAnalyze=typeof analyzeMatches==='function'?analyzeMatches:null;if(!originalAnalyze)return;
  analyzeMatches=async function(matches,meta=null){state.matches=matches;state.meta=meta;const map=await planMap();state.analyses=matches.map(m=>{const raw=window.ArgusEngine.analyze(m),base=window.ArgusGovernance?window.ArgusGovernance.apply(raw,m):raw;return officialFromRow(m,base,map.get(String(m.id)))});render();queueSemantics();archive(matches,state.analyses,meta)};
  window.addEventListener('load',queueSemantics);const obs=new MutationObserver(queueSemantics);if(document.body)obs.observe(document.body,{childList:true,subtree:true,characterData:true});
})();
