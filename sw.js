const CACHE='argus-shell-v3';
const PRECACHE=['/','/live.html','/prediction-results.html','/virtual-bankroll.html','/more.html','/argus-human-v6.css','/argus-os-v1.css','/argus-os-v2.css','/src/argus-os.js'];
self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(PRECACHE)).catch(()=>{}));self.skipWaiting()});
self.addEventListener('activate',e=>{e.waitUntil(Promise.all([caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('argus-shell-')&&k!==CACHE).map(k=>caches.delete(k)))),self.clients.claim()]))});
self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;e.respondWith(fetch(e.request).then(r=>{if(r.ok&&PRECACHE.some(p=>new URL(e.request.url).pathname===p)){const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy)).catch(()=>{})}return r}).catch(()=>caches.match(e.request).then(r=>r||caches.match('/'))))});
self.addEventListener('push',e=>{let d={};try{d=e.data?.json?.()||{}}catch(_){d={body:e.data?.text?.()||''}};const title=d.title||'ARGUS OMNI';const options={body:d.body||'High-signal opportunity detected.',tag:d.tag||'argus-alert',renotify:true,data:{url:d.url||'/daily-slip.html'}};e.waitUntil(self.registration.showNotification(title,options))});
self.addEventListener('notificationclick',e=>{e.notification.close();const url=e.notification.data?.url||'/';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate(url);return c.focus()}}return clients.openWindow(url)}))});
