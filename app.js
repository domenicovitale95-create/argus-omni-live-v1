const $=id=>document.getElementById(id);const FINISHED_STATUSES=new Set(['FT','AET','PEN','CANC','ABD','AWD','WO']);const state={matches:[],analyses:[],mode:'CACHE',meta:null,filter:'signals',sort:'rank'};
function tickClock(){$('clock').textContent=new Date().toLocaleTimeString([],{hour12:false})}setInterval(tickClock,1000);tickClock();
function isPastMatch(m,a){return Boolean(m?.isFinished||FINISHED_STATUSES.has(m?.status)||a?.phase==='FINISHED')}function kickoffText(m){if(m.isLive)return`LIVE ${m.minute||0}'`;if(FINISHED_STATUSES.has(m.status))return'FINAL';if(m.kickoff)return new Date(m.kickoff).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});return m.statusLong||m.status||'PRE-MATCH'}function pct(v){return Number.isFinite(Number(v))?`${(Number(v)*100).toFixed(1)}%`:'—'}function confidenceLabel(v){const n=Number(v)||0;return n>=75?'HIGH':n>=55?'MEDIUM':'LOW'}function betLabel(a){const m=String(a?.bestMarket||'').toUpperCase();if(m==='HOME')return'HOME TEAM TO WIN';if(m==='DRAW')return'DRAW';if(m==='AWAY')return'AWAY TEAM TO WIN';if(m&&!['NO MARKET','MODEL PENDING'].includes(m))return m;return'DO NOT BET'}function signalType(m,a){if(isPastMatch(m,a))return'past';const c=String(a?.classification||'').toUpperCase();if(c.includes('PRIME'))return'prime';if(c.includes('STRONG VALUE'))return'strong-value';if(c.includes('VALUE'))return'value';if(c.includes('WATCH'))return'watch';return'no-bet'}function rankValue(t){return({prime:5,'strong-value':4,value:3,watch:2,'no-bet':1,past:0})[t]||0}function edgeNumber(a){return Number.isFinite(Number(a?.edge))?Number(a.edge):-999}function kickoffNumber(m){return Number(m?.timestamp)||(m?.kickoff?new Date(m.kickoff).getTime()/1000:Number.MAX_SAFE_INTEGER)}function verdictName(t){return t==='prime'?'🟢 PRIME · STRONGEST':(t==='strong-value'||t==='value')?'🔵 VALUE · INTERESTING':t==='watch'?'🟠 WATCH · WAIT':'⚫ NO BET · SKIP'}function reasonText(m,a){if(isPastMatch(m,a))return'Match finished.';if(!a?.marketAvailable)return'No reliable market price available — skip this bet.';if(a?.conservativeEV!=null&&Number(a.conservativeEV)>0)return m.isLive?'The live price still offers positive value according to ARGUS.':'ARGUS estimates this price is better than the underlying probability.';if(signalType(m,a)==='watch')return'Interesting, but not strong enough yet. Wait rather than betting now.';return'Not enough value at the current price. Skip this bet.'}function historyText(m){const h=m.history90d?.home,a=m.history90d?.away;return h&&a?`Recent form: ${Number(h.pointsPerGame||0).toFixed(2)} vs ${Number(a.pointsPerGame||0).toFixed(2)} points/game`:'Recent-history data limited'}function rowsWithTypes(){return state.matches.map((match,index)=>({match,analysis:state.analyses[index],type:signalType(match,state.analyses[index]),index}))}function sortRows(rows){return rows.slice().sort((a,b)=>state.sort==='confidence'?(Number(b.analysis?.confidence)||0)-(Number(a.analysis?.confidence)||0):state.sort==='edge'?edgeNumber(b.analysis)-edgeNumber(a.analysis):state.sort==='kickoff'?kickoffNumber(a.match)-kickoffNumber(b.match):state.sort==='live'?Number(Boolean(b.match.isLive))-Number(Boolean(a.match.isLive))||rankValue(b.type)-rankValue(a.type):rankValue(b.type)-rankValue(a.type)||(Number(b.analysis?.confidence)||0)-(Number(a.analysis?.confidence)||0)||edgeNumber(b.analysis)-edgeNumber(a.analysis)||kickoffNumber(a.match)-kickoffNumber(b.match))}function activeRows(){return rowsWithTypes().filter(r=>r.type!=='past')}function actionableRows(){return sortRows(activeRows().filter(r=>['prime','strong-value','value','watch'].includes(r.type)))}function filteredRows(){let rows=rowsWithTypes();if(state.filter==='past')rows=rows.filter(r=>r.type==='past');else{rows=rows.filter(r=>r.type!=='past');if(state.filter==='signals')rows=rows.filter(r=>['prime','strong-value','value','watch'].includes(r.type));else if(state.filter==='value')rows=rows.filter(r=>['value','strong-value'].includes(r.type));else if(state.filter!=='all')rows=rows.filter(r=>r.type===state.filter)}return sortRows(rows)}
function updateQuota(meta){const q=meta?.quota,remaining=q?.dailyRemaining,limit=q?.dailyLimit,text=remaining==null?'—':limit!=null?`${remaining}/${limit}`:String(remaining);$('requestQuota').textContent=text;$('heroQuota').textContent=text;const safe=remaining!=null&&Number(remaining)<=10;$('safeModeChip').classList.toggle('visible',safe);if(safe)$('scanBtn').textContent=meta?.clientCache?'DATA READY · API PROTECTED':"CHECK TODAY'S BETS"}function updateHistoryCoverage(meta){$('historyCoverage').textContent=meta?.historyTeamsCovered!=null&&meta?.historyTeamsTotal!=null?`${meta.historyTeamsCovered}/${meta.historyTeamsTotal}`:'—'}function updateGovernanceStatus(){const s=window.ArgusGovernance?.systemStatus?.();$('calibrationStatus').textContent=s?.calibration||'UNKNOWN';$('primeGateStatus').textContent=s?.primeGate||'UNKNOWN';$('trackRecordCount').textContent=window.ArgusTrackRecord?.count?.()??0}
function opportunityCard(row,isTop=false){const{match,analysis,type,index}=row,edge=Number.isFinite(Number(analysis.edge))?`${Number(analysis.edge)>=0?'+':''}${Number(analysis.edge).toFixed(1)}%`:'—';return`<article class="opportunity-card ${type} ${isTop?'number-one':''}"><div class="opp-head"><span class="signal-badge ${type}">${isTop?'🏆 #1 TODAY · ':''}${verdictName(type)}</span><span>${kickoffText(match)}</span></div><h4>${match.home} <em>vs</em> ${match.away}</h4><div class="bet-block"><span>${isTop?'BET THIS':'ARGUS SUGGESTS'}</span><strong>${betLabel(analysis)}</strong></div><div class="opp-metrics"><div><span>CONFIDENCE · HOW SURE?</span><strong>${analysis.confidence||0}% · ${confidenceLabel(analysis.confidence)}</strong></div><div title="Value compared with the market price"><span>VALUE ADVANTAGE</span><strong>${edge}</strong></div></div><p>${reasonText(match,analysis)}</p><button class="detail-btn" data-detail-index="${index}">WHY THIS PICK?</button></article>`}
function renderTopOpportunities(){const rows=actionableRows().slice(0,5),top=$('topPick'),grid=$('topOpportunities');if(!rows.length){top.className='top-pick empty-pick panel';top.innerHTML='<div class="pick-copy"><span class="pick-kicker">⛔ ARGUS VERDICT</span><strong>NO BET RIGHT NOW</strong><small>No opportunity is strong enough. The simple action: do not place a bet yet.</small></div>';grid.innerHTML='';return}top.className=`top-pick panel ${rows[0].type}`;top.innerHTML=opportunityCard(rows[0],true);grid.innerHTML=rows.slice(1).map(r=>opportunityCard(r)).join('')}
function cardTemplate(m,a,index){const type=signalType(m,a),past=type==='past',edge=Number.isFinite(Number(a.edge))?`${Number(a.edge)>=0?'+':''}${Number(a.edge).toFixed(1)}%`:'—';if(past)return`<article class="match-card passed panel"><div><h4>${m.home} <span>${m.score?.home??0} — ${m.score?.away??0}</span> ${m.away}</h4><p>Finished match</p></div><div class="passed-tag">FINISHED</div></article>`;return`<article class="match-card panel ${type}"><div class="match-main"><div class="match-topline"><span>${m.competition||'MATCH'} · ${m.country||''}</span><b>${kickoffText(m)}</b></div><h4>${m.home} <em>vs</em> ${m.away}</h4><p>${historyText(m)}</p></div><div class="decision-cell"><span>WHAT TO BET</span><strong>${betLabel(a)}</strong><small>${reasonText(m,a)}</small></div><div class="confidence-cell"><span>ARGUS CONFIDENCE</span><strong>${a.confidence||0}% · ${confidenceLabel(a.confidence)}</strong><div class="confidence-bar"><i style="width:${Math.max(0,Math.min(100,Number(a.confidence)||0))}%"></i></div><small>Value advantage: ${edge}</small></div><div class="signal ${type}"><strong>${verdictName(type)}</strong><small>${type==='watch'?'WAIT':type==='no-bet'?'SKIP':'BET SIGNAL'}</small><button class="detail-btn tiny" data-detail-index="${index}">WHY?</button></div><div class="analysis-detail" id="detail-${index}"><span>Model probability ${pct(a.rawProbability)}</span><span>Adjusted ${pct(a.shrunkProbability)}</span><span>Conservative ${pct(a.conservativeProbability)}</span><span>Value ${a.conservativeEV==null?'—':`${a.conservativeEV}%`}</span><span>${a.engineStatus||'ENGINE STATUS UNKNOWN'}</span><span>${a.shrinkageStatus||''}</span></div></article>`}
function bindButtons(){document.querySelectorAll('[data-detail-index]').forEach(btn=>btn.addEventListener('click',()=>{const el=$(`detail-${btn.dataset.detailIndex}`);if(el)el.classList.toggle('open');else{state.filter='all';renderBoard();setTimeout(()=>{const d=$(`detail-${btn.dataset.detailIndex}`);d?.classList.add('open');d?.scrollIntoView({behavior:'smooth',block:'center'})},0)}}))}function updateCounts(){const rows=rowsWithTypes(),active=rows.filter(r=>r.type!=='past'),prime=active.filter(r=>r.type==='prime').length,value=active.filter(r=>['value','strong-value'].includes(r.type)).length,watch=active.filter(r=>r.type==='watch').length,noBet=active.filter(r=>r.type==='no-bet').length,past=rows.filter(r=>r.type==='past').length;$('signalCount').textContent=prime+value+watch;$('primeFilterCount').textContent=prime;$('valueFilterCount').textContent=value;$('watchFilterCount').textContent=watch;$('noBetFilterCount').textContent=noBet;$('allFilterCount').textContent=active.length;$('pastFilterCount').textContent=past;$('heroAnalysed').textContent=active.length;$('heroPrime').textContent=prime;$('heroWatch').textContent=watch;$('todayCounts').textContent=`${prime} BEST · ${value} VALUE · ${watch} WAIT · ${noBet} SKIP · ${past} FINISHED`;const best=actionableRows()[0];$('strongestSignal').textContent=best?`Best now: ${best.match.home} vs ${best.match.away} → ${betLabel(best.analysis)} (${best.analysis.confidence||0}% confidence)`:'Simple answer: NO BET right now.'}function renderBoard(){updateCounts();renderTopOpportunities();const grid=$('matchGrid'),rows=filteredRows();if(!state.matches.length){grid.innerHTML='<div class="empty-state"><strong>NO DATA LOADED</strong><span>Press CHECK TODAY\'S BETS.</span></div>';return}if(!rows.length){grid.innerHTML='<div class="empty-state"><strong>NO BET RIGHT NOW</strong><span>ARGUS found no sufficiently strong opportunity. Do not force a bet.</span></div>';return}grid.innerHTML=rows.map(r=>cardTemplate(r.match,r.analysis,r.index)).join('');bindButtons()}function render(){updateQuota(state.meta);updateHistoryCoverage(state.meta);updateGovernanceStatus();const stamp=state.meta?.fetchedAt?new Date(state.meta.fetchedAt):null,t=stamp?stamp.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):'—';$('heroUpdate').textContent=t;$('lastUpdate').textContent=stamp?`Updated ${t}${state.meta?.clientCache?' · CACHED':''}`:'Awaiting scan';renderBoard()}function analyzeMatches(matches,meta=null){state.matches=matches;state.meta=meta;state.analyses=matches.map(m=>{const base=window.ArgusEngine.analyze(m);return window.ArgusGovernance?window.ArgusGovernance.apply(base,m):base});render()}
async function scanToday(){$('scanBtn').disabled=true;const cache=window.ArgusProviders.cacheStatus?.();$('scanBtn').textContent=cache?.safeMode&&cache?.fresh?'DATA READY · API PROTECTED':'CHECKING…';try{const matches=await window.ArgusProviders.live({force:false});state.mode=matches.meta?.clientCache?'CACHE':'TODAY';analyzeMatches(matches,matches.meta||null);$('liveStatus').textContent=matches.meta?.clientCache?'CACHED':'CONNECTED'}catch(e){$('liveStatus').textContent='ERROR';alert(e.message)}finally{$('scanBtn').disabled=false;const c=window.ArgusProviders.cacheStatus?.();$('scanBtn').textContent=c?.safeMode&&c?.fresh?'DATA READY · API PROTECTED':"CHECK TODAY'S BETS"}}async function detectLiveBackend(){const s=await window.ArgusProviders.health();$('liveStatus').textContent=s.cached?'CACHE READY':'READY';if(s.meta){updateQuota(s.meta);updateHistoryCoverage(s.meta)}if(Array.isArray(s.matches)&&s.matches.length){const m=s.matches.slice();m.meta=s.meta||null;state.mode='CACHE';analyzeMatches(m,s.meta||null)}else render()}function setFilter(f){state.filter=f;document.querySelectorAll('.filter-btn').forEach(b=>b.classList.toggle('active',b.dataset.filter===f));renderBoard()}$('scanBtn').addEventListener('click',scanToday);document.querySelectorAll('.filter-btn').forEach(b=>b.addEventListener('click',()=>setFilter(b.dataset.filter)));$('sortSelect').addEventListener('change',e=>{state.sort=e.target.value;renderBoard()});detectLiveBackend();